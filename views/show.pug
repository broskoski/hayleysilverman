extends layout

block content
  .BlockContainer
    if image
      .Block(style="background-image: url(" + image.original.url + ")")

    if description_html
      .BlockMetadata
        .BlockMetadata__description
          != helpers.addLinkTarget(description_html)

    a.BlockBack(href="/#" + id) ×
    a.BlockDown ⌄